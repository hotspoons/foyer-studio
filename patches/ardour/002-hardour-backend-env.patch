diff --git a/headless/load_session.cc b/headless/load_session.cc
index f921b83676..16901bcbb1 100644
--- a/headless/load_session.cc
+++ b/headless/load_session.cc
@@ -72,8 +72,11 @@ load_session (string dir, string state)
 
 	AudioEngine* engine = AudioEngine::create ();
 
-	if (!engine->set_backend (backend_name, backend_client_name, "")) {
-		std::cerr << "Cannot set Audio/MIDI engine backend\n";
+	const char* backend_override = getenv ("ARDOUR_BACKEND");
+	string resolved_backend = backend_override ? backend_override : backend_name;
+
+	if (!engine->set_backend (resolved_backend, backend_client_name, "")) {
+		std::cerr << "Cannot set Audio/MIDI engine backend: " << resolved_backend << "\n";
 		exit (EXIT_FAILURE);
 	}
 
